<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_admin_course_path(@course), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @course.title %></h2>
  <p><%= @course.description %></p>
</div>
